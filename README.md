* Lux2 bot that will parrot back the actions from the replay file specified in the config.
* For this to be useful, you need to send the right seed when starting Lux.
* It also rather requires the other bot to be deterministic.
